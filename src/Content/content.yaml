interface:
  - name: IContentProps

    properties:
      - name: id
        type: string
        
      - name: header
        type: string

      - name: children
        type: ReactNode

name: Content
description: A wrapper for the content of each section

props:
  type: IContentProps

contents:
  - name: Section Header
    description: A header for the section

    text: {header}

template: React Fragment
